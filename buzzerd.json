{
  "default": {
    "led": [0, 40, 127],
    "press": { "shell": "osascript -e 'set volume output muted (not output muted of (get volume settings))'" },
    "wheelUp": { "shell": "osascript -e 'set volume output volume ((output volume of (get volume settings)) + 4)'" },
    "wheelDown": { "shell": "osascript -e 'set volume output volume ((output volume of (get volume settings)) - 4)'" }
  },
  "com.spotify.client": {
    "led": [0, 127, 0],
    "press": { "shell": "osascript -e 'tell application \"Spotify\" to playpause'" }
  }
}
